import React from "react";
import classes from "./CreateRoutine.module.css";

const createRoutine = (props) => {
  return <div className={classes.CreateRoutine}>Click to add routine!</div>;
};

export default createRoutine;
